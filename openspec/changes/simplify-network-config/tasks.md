## 1. Implementation
- [x] 1.1 Implement compact loading and centralized rendering; verify count, validation, deterministic peers and snapshot tests.
- [x] 1.2 Consolidate deployment resources and migrate defaults/docs; verify networks contains only local.yaml and old active references are gone.
## 2. Validation
- [x] 2.1 Run network and command tests and render the repository configuration without starting containers; validate OpenSpec.
